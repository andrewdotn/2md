#!/bin/bash

set -eu

PROG="${0}"
if [ -h "${PROG}" ]; then
    PROG="$(readlink -- "${PROG}")"
fi

DIR="$(dirname -- "${PROG}")/.."
DIR="$(cd "${DIR}" && pwd -P)"

exec node "${DIR}/src/main.ts" \
    "${@+"${@}"}"
