{
  "name": "2md",
  "version": "0.0.0",
  "license": "Apache-2.0",
  "scripts": {
    "test": "tsc && yarn run pretty:check && mocha -r ./babel-register --exclude 'node_modules/**' 'src/**/*-test.ts'",
    "pretty": "prettier --write '*.js' '*.json' 'src/**/*.ts'",
    "pretty:check": "prettier --check '*.js' '*.json' 'src/**/*.ts'"
  },
  "devDependencies": {
    "@types/fs-extra": "^8.0.0",
    "@types/jsdom": "^12.2.4",
    "mocha": "^6.2.1",
    "prettier": "^1.18.2"
  },
  "dependencies": {
    "fs-extra": "^8.1.0",
    "jsdom": "^15.1.1"
  }
}
